# Add project specific ProGuard rules here.
# Stub module — R8/minify currently disabled (release buildType isMinifyEnabled=false).
# :core supplies its own consumer-rules.pro covering SheetPayload serialization.
